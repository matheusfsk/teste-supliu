import styled from "styled-components";

export const Container = styled.div`
  > button {
    border: none;
    background-color: transparent;
    display: inline-flex;
    color: red;
    > svg {
      width: 22px;
      height: 24px;
    }
  }
`;
